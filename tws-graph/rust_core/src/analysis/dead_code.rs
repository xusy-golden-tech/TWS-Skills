//! Dead code detection — identifies symbols with zero inbound references.

/// Placeholder for dead-code analysis.
pub fn detect() -> Vec<String> {
    // TODO: query graph for symbols with no inbound CALLS / REFERENCES edges
    Vec::new()
}
