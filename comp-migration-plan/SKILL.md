---
name: migration-plan
description: 迁移/重构计划。把大改动拆成可独立验证的小步，每步可回退。禁止大爆炸式重构
---

# 迁移计划

## 核心原则

**一步改完的不是重构，是重写。重构是一小步一小步走过去的。**

## Step 0: 图中分析依赖

制定迁移步骤之前，必须先用代码图理清模块间的依赖关系。

**必须操作：** 通过 Skill 工具加载 `found-tws-graph-usage`（Skill(skill: "found-tws-graph-usage")），按其中指引完成前置检查和以下查询：

```
1. tws-graph --version              ← 检查可用性（不可用则 pip install -e tws-graph/）

2. tws-graph impact <待迁移模块> --depth 3  ← 查完整依赖树，识别所有受影响模块

3. tws-graph calls <模块> --inbound         ← 查反向依赖，确认迁移不会断链
```

错误处理详见 `found-tws-graph-usage`。图返回空时标注 provenance=heuristic，退回到 Grep + Read 手动追踪。

## The Gate Function

```
1. 识别当前状态（代码结构、依赖关系）
2. 确定目标状态（目标结构、接口设计）
3. 找出一条从当前到目标的路径，拆成小步
4. 每步必须是：
   - 可独立验证（跑测试确认没改坏）
   - 可回退（出问题了可以回到上一步）
   - 不产生中间状态的编译错误
5. 按执行顺序排列迁移步骤
```

## 输出格式

```markdown
### 迁移步骤
1. {模块A}重构 → 验证对等 → 可回退
2. {模块B}重构 → 验证对等 → 可回退
...
```

## 不可行的分解

```
❌ 先改 A 再改 B，但 A 改完跑不了 → 改法不对，重新分解
❌ 一步改了 10 个文件 → 拆太小了没意义 → 拆大
```

## Rationalization Prevention

| 想说的话 | 真相 |
|---------|------|
| 「重构完一次性验证」 | 一次性验证 = 出问题了不知道是哪一步引起的 |
| 「这个改动很小，不用每步回退」 | 任何一步出问题都不知道回哪去 |
| 「重构和加功能一起做了」 | 重构不加功能，加功能不改结构 |
