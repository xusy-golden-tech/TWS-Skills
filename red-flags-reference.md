# 反 Rationalization 参考 — 防抄近路话术手册

> 通用 + 国内模型特化。所有 TWS skill 引用此文档中的规则。

---

## 通用 Pattern（所有模型通用）

| Agent 想说的话 | 真相 | 应对 |
|--------------|------|------|
| "This doesn't need a skill" | 如果 skill 存在就该用 | 调 skill |
| "I remember this" | Skill 会更新 | 读当前版本 |
| "Let me check the code first" | Skill 教你怎么查 | 先调 skill |
| "This feels right" | 感觉 ≠ 证据 | 跑测试 |
| "I'll do this one thing first" | 顺序错了 | 先调 skill |

---

## 国内模型特化 Pattern

国内模型（GLM / DeepSeek / Qwen / Kimi 等）在开发场景中容易出现以下特有模式：

### P01 — 「实际上更简单的做法是」

```
Agent：实际上更简单的做法是直接返回空数据，不用改数据库

真相：这不是「更简单」，是「不解决问题」。
表面简化 = 绕过问题，不是解决问题。
```

**应对：** 检查简化方案是否真的覆盖了需求。如果绕过了核心逻辑，说明是在逃避。

### P02 — 「先这样跑起来再说」

```
Agent：先这样跑起来吧，后面再优化

真相：「后面」= 永远不会。
跳过验证/优化的代码会变成技术债。每拖延一次就多一笔。
```

**应对：** 「后面」不是一个时间点。现在不改就记入问题列表，让开发者决定排期。

### P03 — 「这个问题我见过，就是...」

```
Agent：这个问题我见过，就是 XX 原因

真相：没有检查就下结论 = 猜测，不是诊断。
过度自信导致跳过了排查步骤。
```

**应对：** 必须验证猜测后再输出结论。先确认再判断。

### P04 — 「看起来是对的」

```
Agent：看起来是对的，应该没问题

真相：「看起来」不是验证。代码能看对但跑错。
视觉判断不能代替测试运行。
```

**应对：** 必须运行测试。所有「看起来」类表述自动触发验证要求。

### P05 — 「这个不需要改」

```
Agent：这个不需要改，现有的就能用

真相：Agent 在替开发者做范围判断。
除非有明确证据表明不需要改，否则默认需要改。
```

**应对：** 说出不需要改的具体理由，让开发者判断，而不是自行决定。

### P06 — 「先注释掉，后面再补」

```
Agent：这段逻辑先注释掉，后面再补实现

真相：注释掉的代码 = 没有代码。
「后面再补」的注释代码永远不会被补回来。
```

**应对：** 要么现在实现，要么删掉并记入问题列表。禁止留注释占位。

### P07 — 「也可以不改」

```
Agent：这个地方也可以不改，影响不大

真相：Agent 在替开发者评估影响。
「影响不大」是一个价值判断，不是技术判断。
```

**应对：** 说明影响是什么，让开发者决定改不改。

### P08 — 「这个问题很简单的」

```
Agent：这个问题很简单的，几分钟就能搞定

真相：低估复杂度 = 不充分准备。
说「简单」的 bug 往往藏着最深的坑。
```

**应对：** 不管简不简单，走完整流程。越说简单越要检查。

### P09 — 「大家都这么写的」

```
Agent：这里大家都这么写的，不用改

真相：从众 ≠ 正确。
现有代码可能本身就是错的，或者是历史遗留问题。
```

**应对：** 检查代码是否真的正确，而不是看有多少人这么写。

### P10 — 「建议由开发者决定」

```
Agent：这个建议由开发者自己决定

真相：过度推诿——不给分析直接甩锅。
```

**区分：**
- ✅ 输出分析 + 给出明确建议 + 说明理由 → 这是正确的「让开发者决定」
- ❌ 只说「建议由开发者决定」但不给分析和建议 → 这是在甩锅

**应对：** 先完整分析，再给明确建议，最后说明理由。只说「建议开发者决定」等于没分析。

---

## 使用方式

各 skill 文件通过以下方式引用本手册中的规则：

```markdown
## Rationalization Prevention

| 想说的话 | 真相 |
|---------|------|
| "这个不需要改" | 说出理由，让开发者判断 |
| "看起来是对的" | 运行测试，用输出来说 |
```

参考 `red-flags-reference.md` 中的国内模型特化 Pattern（P01-P10），按技能场景选取相关的加入。
