# Session Checkpoint — 会话状态记录

> 说明文件，非独立 skill。各流程引用此文件中的机制。

## 作用

```
让一次没跑完的流程可以在下次会话中续上继续。支持多个流程并行。
```

## 存储位置

```
.tws/sessions/                          ← 所有 session 文件的目录
  fix-bug-plugin-stuck.md               ← 每个流程一个文件
  add-feature-refresh-btn.md            ← 文件名 = {flow-type}-{short-desc}.md
  refactor-auth.md
```

## 存储格式

每个 session 文件内容：

```markdown
## 当前流程
- 流程名：{添加功能 / 修复 Bug / ...}
- flow skill：{flow-fix-bug / flow-add-feature / ...}
- 模式：Solo / Team
- 开始时间：YYYY-MM-DD HH:MM
- 版本号：{整数，每次写入 +1}

## 进度
- [x] ① {已完成的步骤}
- [ ] ② {当前步骤}
- [ ] ③ {后续步骤}

## 当前任务
- 正在做：{步骤名}
- 状态：进行中 / 阻塞
- 阻塞原因：（无 / 原因描述）

## 阶段交接（下一个子 agent 需要知道的）
- 上一阶段产出：{一句话描述}
- 关键决策：{最多 3 条，格式：决策内容 + 理由}
- 需要加载的文件：{设计书路径、相关代码路径}
- 需要加载的 skill：{comp-xxx}
- 已知风险：{如有}

## 待补事项
- （无 / 遗留项列表）
```

**版本号规则：** 写入前读取当前版本号。如果读到的版本号比记忆中的高，说明有其他 agent 更新过，需要合并后再写入。

**合并策略：**
- 读取当前版本号和内容
- 如果版本号高于预期，说明有其他 agent 更新过
- 合并方式：保留其他 agent 的进度，追加当前 agent 的进度
- 如果进度冲突（同一步骤状态不同），以最新版本为准，当前 agent 的变更记入"待补事项"

## 恢复流程

```
下次会话启动时（using-tws 第一步）：
1. 用 Glob 检查 .tws/sessions/*.md
2. 没有文件 → 正常路由
3. 有 1 个文件 → 询问用户是否续上
4. 有多个文件 → 列出所有，让用户选择续上哪个或开始新流程
5. 用户选择续上 → 读取对应文件，加载 flow skill，从断点继续
```

## 保存时机

```
流程启动时（用户确认后）→ Write 创建 .tws/sessions/{name}.md
每完成一个步骤 → Edit 更新（[ ]→[x]，版本号+1）
遇到阻塞时 → Edit 更新状态 + 标注阻塞原因
流程完成时 → 删除对应 session 文件
```

## 标准完成依据

所有 flow 默认满足以下条件才算完成：

```markdown
- [ ] `.tws/sessions/{当前流程文件}` 已删除（流程完成，清理断点文件）
- [ ] 已确认无阻塞项或阻塞项已转入 `.tws/deferred-issues.md`
- [ ] 若流程产生待补事项，已写入 session / deferred 文件或用户已确认处理方式
```

各 flow 的 `## 完成依据` 只写本流程额外要求，不重复以上公共项。

## 全量回退

执行到一半发现方向完全错了：

```
1. git stash 或 git reset --hard 回到流程开始前的状态
2. 删除 .tws/sessions/{当前流程文件}
3. 评估是否重新开始
4. 注意：代码已提交到共享分支时，优先用 git revert 替代 git reset
```

## 损坏恢复

如果某个 session 文件格式损坏或无法解析：

```
1. 不崩溃，不终止流程
2. 输出：「⚠ session 文件 {filename} 格式异常，无法自动恢复。」
3. 询问用户：「要删除这个文件重新开始，还是手动修复后继续？」
4. 用户选删除 → 删除该文件，正常路由
5. 用户选修复 → 等待用户操作后重试读取
```
